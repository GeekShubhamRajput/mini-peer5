<h1>New User</h1>
<%= form_with model: @user, local: true do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.submit 'Save' %>
  <%= link_to 'Cancel', users_path %>
<% end %>
